# HolmesGPT 프로젝트 심층 분석

> HolmesGPT는 클라우드 인프라 문제를 자동으로 조사하고 근본 원인을 분석하는 AI 에이전트입니다.
> CNCF Sandbox 프로젝트로, Robusta.dev에서 개발되었습니다.

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [아키텍처](#2-아키텍처)
3. [에이전트 동작 방식](#3-에이전트-동작-방식)
4. [Toolset 시스템](#4-toolset-시스템)
5. [통합 기능](#5-통합-기능)
6. [Runbook 시스템](#6-runbook-시스템)
7. [사용법](#7-사용법)
8. [K8sGPT/Kagent와 비교](#8-k8sgptkagent와-비교)
9. [결론](#9-결론)

---

## 1. 프로젝트 개요

### 1.1 HolmesGPT란?

HolmesGPT는 **24/7 온콜 AI 에이전트**입니다. 알람이 발생하면 자동으로 관련 데이터를 수집하고, 근본 원인을 분석하며, 해결 방안을 제시합니다.

**핵심 가치:**
- **자동 조사**: 알람 발생 시 자동으로 로그, 메트릭, 이벤트 수집
- **근본 원인 분석**: LLM을 활용한 지능적 분석
- **양방향 통합**: 분석 결과를 PagerDuty/Jira/Slack에 자동 기록
- **읽기 전용**: 시스템 수정 없이 안전하게 조사

### 1.2 CNCF 프로젝트

- **상태**: CNCF Sandbox (2025년 10월 승인)
- **원 개발사**: Robusta.dev
- **GitHub**: [HolmesGPT/holmesgpt](https://github.com/HolmesGPT/holmesgpt)

### 1.3 주요 특징

| 특징 | 설명 |
|------|------|
| **50+ Toolsets** | K8s, Prometheus, Grafana, AWS, Datadog 등 |
| **다중 알람 소스** | AlertManager, PagerDuty, OpsGenie, Jira |
| **다중 LLM** | OpenAI, Anthropic, Azure, Bedrock, Ollama |
| **MCP 지원** | Model Context Protocol로 확장 가능 |
| **Runbook** | 사전 정의된 조사 지침 적용 |

---

## 2. 아키텍처

### 2.1 전체 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                    HolmesGPT 아키텍처                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   알람 발생 (PagerDuty/OpsGenie/AlertManager)                   │
│        │                                                         │
│        ▼                                                         │
│   ┌──────────────────────────────────────────────────────────┐  │
│   │                    HolmesGPT CLI                          │  │
│   │                                                           │  │
│   │  1. 알람 수신 및 Runbook 매칭                             │  │
│   │  2. 시스템 프롬프트 + Runbook 지침 구성                   │  │
│   │  3. 에이전트 루프 시작                                    │  │
│   │                                                           │  │
│   │     ┌─────────────────────────────────────────────────┐   │  │
│   │     │              에이전트 루프 (최대 40 스텝)         │   │  │
│   │     │                                                  │   │  │
│   │     │  ┌─────────┐      ┌──────────────┐             │   │  │
│   │     │  │   LLM   │ ───▶ │ "kubectl로   │             │   │  │
│   │     │  │         │      │  로그 확인"  │             │   │  │
│   │     │  └─────────┘      └──────────────┘             │   │  │
│   │     │       ▲                  │                      │   │  │
│   │     │       │                  ▼ tool_call            │   │  │
│   │     │       │           ┌──────────────┐             │   │  │
│   │     │       │           │  Toolset     │             │   │  │
│   │     │       │           │  (kubectl,   │             │   │  │
│   │     │       │           │   promql,    │             │   │  │
│   │     │       │           │   loki...)   │             │   │  │
│   │     │       │           └──────────────┘             │   │  │
│   │     │       │                  │                      │   │  │
│   │     │       └──── 결과 반환 ───┘                      │   │  │
│   │     │                                                  │   │  │
│   │     │  반복: LLM이 "충분한 정보 확보" 판단까지         │   │  │
│   │     └─────────────────────────────────────────────────┘   │  │
│   │                                                           │  │
│   │  4. 구조화된 분석 결과 생성                               │  │
│   └──────────────────────────────────────────────────────────┘  │
│        │                                                         │
│        ▼                                                         │
│   결과 전송 (Slack/PagerDuty/Jira에 자동 기록)                  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 디렉토리 구조

```
holmesgpt/
├── holmes/
│   ├── main.py              # CLI 진입점 (Typer)
│   ├── config.py            # 설정 관리
│   ├── interactive.py       # 대화형 모드
│   │
│   ├── core/
│   │   ├── tool_calling_llm.py    # 에이전트 루프 핵심
│   │   ├── investigation.py       # 조사 오케스트레이션
│   │   ├── tools.py               # 도구 정의
│   │   ├── toolset_manager.py     # Toolset 관리
│   │   ├── llm.py                 # LLM 추상화 (LiteLLM)
│   │   ├── runbooks.py            # Runbook 관리
│   │   └── prompt.py              # 프롬프트 생성
│   │
│   └── plugins/
│       ├── sources/         # 알람 소스 (AlertManager, PagerDuty...)
│       ├── destinations/    # 결과 전송 (Slack)
│       ├── toolsets/        # 도구 정의 (50+)
│       ├── prompts/         # Jinja2 프롬프트 템플릿
│       └── runbooks/        # 내장 Runbook
│
├── helm/                    # K8s 배포 차트
├── docs/                    # 문서
└── tests/                   # 테스트
```

---

## 3. 에이전트 동작 방식

### 3.1 핵심 흐름: ToolCallingLLM

```python
# core/tool_calling_llm.py (간소화)

class ToolCallingLLM:
    def ask(self, prompt: str) -> LLMResult:
        messages = [{"role": "user", "content": prompt}]

        for step in range(self.max_steps):  # 최대 40 스텝
            # 1. LLM 호출 (도구 목록 전달)
            response = self.llm.create(
                messages=messages,
                tools=self.available_tools
            )

            # 2. LLM이 도구 호출 결정?
            if response.finish_reason == "tool_calls":
                for tool_call in response.tool_calls:
                    # 3. 도구 실행
                    result = self.execute_tool(
                        tool_call.name,
                        tool_call.arguments
                    )

                    # 4. 결과를 메시지에 추가
                    messages.append({
                        "role": "tool",
                        "tool_call_id": tool_call.id,
                        "content": result
                    })

            # 5. LLM이 결론 도달?
            elif response.finish_reason == "stop":
                return LLMResult(result=response.content)

        return LLMResult(result="Max steps reached")
```

### 3.2 구체적 예시

```
사용자: "왜 frontend Pod가 CrashLoopBackOff인가?"

┌─────────────────────────────────────────────────────────────┐
│ Step 1: LLM 판단                                            │
│ "Pod 상태를 확인해야겠다"                                   │
│ → tool_call: kubectl_get_by_name(kind=pod, name=frontend)   │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 도구 실행                                           │
│ $ kubectl get pod frontend -o wide                          │
│ 결과: "frontend  0/1  CrashLoopBackOff  5  10m"            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: LLM 판단                                            │
│ "로그를 확인해야겠다"                                       │
│ → tool_call: kubectl_logs(pod=frontend, previous=true)      │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: 도구 실행                                           │
│ $ kubectl logs frontend --previous                          │
│ 결과: "Error: FATAL: database connection refused"          │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 5: LLM 판단                                            │
│ "DB 서비스 상태를 확인해야겠다"                             │
│ → tool_call: kubectl_get_by_name(kind=service, name=db)     │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 6: LLM 결론                                            │
│ "충분한 정보를 수집했다. 근본 원인 분석 완료"               │
│                                                              │
│ **분석 결과:**                                              │
│ frontend Pod가 CrashLoopBackOff 상태입니다.                 │
│ 로그에서 "database connection refused" 에러가 발견됨.       │
│ DB 서비스가 존재하지 않거나 접근 불가 상태입니다.           │
│                                                              │
│ **권장 조치:**                                              │
│ 1. DB 서비스 배포 상태 확인                                 │
│ 2. NetworkPolicy로 인한 차단 여부 확인                      │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 K8sGPT/Kagent와의 핵심 차이

```
┌─────────────────────────────────────────────────────────────┐
│                    K8sGPT (규칙 기반)                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   [하드코딩된 분석기] ──▶ [문제 감지] ──▶ [LLM: 설명 생성]  │
│                                                              │
│   LLM은 "설명만" 생성, 도구 선택권 없음                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    HolmesGPT (에이전트)                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   [LLM] ──▶ "어떤 도구가 필요한가?" ──▶ [도구 선택]        │
│     ▲                                       │                │
│     │                                       ▼                │
│     └────────── [결과 분석] ◀────── [도구 실행]             │
│                                                              │
│   LLM이 "도구 선택 + 실행 + 분석" 전 과정 주도              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. Toolset 시스템

### 4.1 내장 Toolsets (50+)

| 카테고리 | Toolset | 기능 |
|----------|---------|------|
| **Kubernetes** | kubernetes/core | Pod, Service, Deployment 조회 |
| | kubernetes_logs | 컨테이너 로그 |
| | aks | Azure Kubernetes Service |
| **메트릭** | prometheus | PromQL 쿼리 |
| | datadog | 로그, 메트릭, 추적 |
| | newrelic | 알림, 추적 |
| | grafana | 대시보드, Loki, Tempo |
| **로깅** | loki | 구조화된 로그 쿼리 |
| | elasticsearch | 클러스터 상태, 로그 |
| | coralogix | 로그 데이터 |
| **메시지 큐** | kafka | 토픽, 컨슈머 그룹 |
| | rabbitmq | 큐 상태 |
| **클라우드** | aws | RDS, EC2, CloudWatch |
| | azure_sql | Azure SQL |
| **CI/CD** | argocd | 앱 상태, 배포 이력 |
| | github (MCP) | PR, Issues, Actions |
| **기타** | docker | 컨테이너 상태 |
| | helm | 릴리스 상태 |
| | confluence | 문서 검색 |

### 4.2 Toolset 정의 방식

**YAML 기반 (간단한 도구):**

```yaml
# plugins/toolsets/kubernetes.yaml
toolsets:
  kubernetes/core:
    description: "K8s 리소스 조회"
    prerequisites:
      - command: "kubectl version --client"

    tools:
      - name: "kubectl_describe"
        description: "kubectl describe 실행"
        command: "kubectl describe {{ kind }} {{ name }}{% if namespace %} -n {{ namespace }}{% endif %}"

        # 긴 출력 자동 요약
        transformers:
          - name: llm_summarize
            config:
              input_threshold: 1000
              prompt: "핵심 정보만 요약..."

      - name: "kubectl_logs"
        description: "Pod 로그 조회"
        command: "kubectl logs {{ pod }}{% if previous %} --previous{% endif %}{% if tail %} --tail={{ tail }}{% endif %}"
```

**Python 기반 (복잡한 도구):**

```python
# plugins/toolsets/prometheus/prometheus.py
class PrometheusToolset(Toolset):
    def __init__(self, config):
        self.url = config.get("url", "http://prometheus:9090")

    @property
    def tools(self):
        return [
            Tool(
                name="execute_promql",
                description="PromQL 쿼리 실행",
                func=self.execute_query,
                parameters=[
                    {"name": "query", "type": "string", "required": True},
                    {"name": "time", "type": "string", "required": False}
                ]
            )
        ]

    def execute_query(self, query: str, time: str = None):
        response = requests.get(
            f"{self.url}/api/v1/query",
            params={"query": query, "time": time}
        )
        return StructuredToolResult(data=response.json())
```

### 4.3 Transformer 시스템

긴 도구 출력을 자동으로 요약하여 컨텍스트 윈도우 초과를 방지합니다.

```yaml
tools:
  - name: "kubectl_describe"
    transformers:
      - name: llm_summarize
        config:
          input_threshold: 1000  # 1000 토큰 초과시 요약
          model: "gpt-4o-mini"   # 빠른 모델 사용
          prompt: |
            다음 정보에서 핵심만 추출:
            - 오류 및 경고
            - 리소스 상태
            - 중요 이벤트
```

### 4.4 MCP 서버 지원

```yaml
# config.yaml
mcp_servers:
  github:
    command: /path/to/github-mcp-server
    args: ["--token", "${GITHUB_TOKEN}"]
```

HolmesGPT가 자동으로 MCP 서버의 도구를 검색하고 사용합니다.

---

## 5. 통합 기능

### 5.1 알람 소스 (조사 대상)

```
┌─────────────────────────────────────────────────────────────┐
│                     알람 소스                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  AlertManager ─────▶ holmes investigate alertmanager        │
│                      --alertmanager-url http://localhost:9093│
│                                                              │
│  PagerDuty ────────▶ holmes investigate pagerduty           │
│                      --pagerduty-api-key XXXX               │
│                                                              │
│  OpsGenie ─────────▶ holmes investigate opsgenie            │
│                      --opsgenie-api-key XXXX                │
│                                                              │
│  Jira ─────────────▶ holmes investigate jira                │
│                      --jira-url https://...                 │
│                      --jira-username ...                    │
│                      --jira-api-key ...                     │
│                                                              │
│  GitHub Issues ────▶ holmes investigate github              │
│                      --github-owner ...                     │
│                      --github-pat ghp_...                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 결과 전송 (양방향 통합)

```bash
# AlertManager 조사 → Slack에 결과 전송
holmes investigate alertmanager \
  --alertmanager-url http://localhost:9093 \
  --destination slack \
  --slack-token xoxb-... \
  --slack-channel "#alerts"

# PagerDuty 조사 → 인시던트에 분석 결과 추가
holmes investigate pagerduty \
  --pagerduty-api-key XXXX \
  --update  # 인시던트에 코멘트로 분석 결과 추가

# JSON 파일로 저장
holmes ask "..." --json-output-file result.json
```

### 5.3 Slack 통합

```
┌─────────────────────────────────────────────────────────────┐
│                   Slack 통합 방식                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  방식 1: 결과 전송 (Destination)                            │
│  ─────────────────────────────────                          │
│  holmes ask "..." --destination slack                       │
│  → 분석 결과가 Slack 채널에 자동 전송                       │
│                                                              │
│  방식 2: 대화형 봇 (Beta)                                   │
│  ─────────────────────────────                              │
│  Slack에서: @Holmes 왜 Pod가 죽었어?                        │
│  → Holmes가 자동으로 조사 후 답변                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. Runbook 시스템

### 6.1 Runbook이란?

Runbook은 **특정 알람에 대한 조사 지침**입니다. LLM이 무작정 조사하는 대신, 사전 정의된 절차를 따릅니다.

```
┌─────────────────────────────────────────────────────────────┐
│                   Runbook 적용 흐름                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  알람: "KubePodCrashLooping"                                │
│        │                                                     │
│        ▼                                                     │
│  Runbook 매칭: issue_name =~ "CrashLoop"                    │
│        │                                                     │
│        ▼                                                     │
│  지침 추가:                                                  │
│  "1. Pod 로그 확인                                          │
│   2. OOMKilled 여부 확인                                    │
│   3. 최근 배포 여부 확인                                    │
│   4. 리소스 제한 검토"                                      │
│        │                                                     │
│        ▼                                                     │
│  LLM이 지침에 따라 체계적 조사                              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 Runbook 정의

```yaml
# custom_runbooks.yaml
runbooks:
  # 알람 이름으로 매칭
  - match:
      issue_name: "(KubePodCrashLooping)|(KubePodNotReady)"
    instructions: |
      Pod 크래시 조사 절차:

      1단계: Pod 상태 확인
      - 현재 상태와 재시작 횟수 확인
      - 마지막 재시작 시간 기록

      2단계: 로그 분석
      - 현재 로그와 이전 로그 비교
      - OOMKilled vs 애플리케이션 에러 구분

      3단계: 리소스 확인
      - CPU/메모리 리밋 검토
      - 노드 리소스 압박 상태 확인

      4단계: 최근 변경 확인
      - 최근 배포 여부
      - 환경 변수 변경 여부

  # 소스별 매칭
  - match:
      source: "prometheus"
      alert_name: "HighErrorRate"
    instructions: |
      에러율 급증 조사:
      1. 서비스 로그에서 에러 패턴 확인
      2. 최근 배포 여부 확인
      3. 외부 서비스 상태 확인
```

### 6.3 Runbook 사용

```bash
# 커스텀 Runbook 적용
holmes ask "왜 Pod가 죽었어?" -r ./my_runbooks.yaml

# 여러 Runbook 적용
holmes ask "..." -r ./runbook1.yaml -r ./runbook2.yaml
```

---

## 7. 사용법

### 7.1 설치

```bash
# pip 설치 (권장)
pip install holmesgpt

# Homebrew (macOS)
brew install holmesgpt

# Docker
docker run -e OPENAI_API_KEY=sk-... holmesgpt/holmes ask "..."

# Kubernetes Helm
helm repo add robusta https://robusta.dev/helm
helm install holmes robusta/holmes -n default
```

### 7.2 기본 설정

```bash
# 환경 변수 설정
export OPENAI_API_KEY="sk-..."
# 또는
export ANTHROPIC_API_KEY="sk-ant-..."
```

또는 config.yaml:

```yaml
# ~/.holmes/config.yaml
model: "gpt-4.1"
api_key: "${OPENAI_API_KEY}"  # 환경변수 참조

# Kubernetes 클러스터 이름
cluster_name: "prod-cluster"

# Toolset 활성화
toolsets:
  kubernetes/core:
    enabled: true
  prometheus/metrics:
    enabled: true
    config:
      url: "http://prometheus:9090"
```

### 7.3 CLI 명령어

#### ask - 일반 질문

```bash
# 기본 사용
holmes ask "왜 frontend Pod가 CrashLoopBackOff인가?"

# 대화형 모드
holmes ask "클러스터 상태 분석해줘" --interactive

# 파일 첨부
holmes ask "이 로그 분석해줘" -f ./error.log

# 모델 지정
holmes ask "..." --model anthropic/claude-sonnet-4-20250514

# 커스텀 도구셋 추가
holmes ask "..." -t ./custom_toolset.yaml

# Runbook 추가
holmes ask "..." -r ./custom_runbooks.yaml

# 결과를 Slack으로 전송
holmes ask "..." --destination slack --slack-token xoxb-... --slack-channel "#alerts"

# 비용 추적
holmes ask "..." --log-costs
```

#### investigate - 알람 조사

```bash
# AlertManager
holmes investigate alertmanager \
  --alertmanager-url http://localhost:9093

# PagerDuty (분석 결과를 인시던트에 추가)
holmes investigate pagerduty \
  --pagerduty-api-key XXXX \
  --update

# OpsGenie
holmes investigate opsgenie \
  --opsgenie-api-key XXXX

# Jira
holmes investigate jira \
  --jira-url https://company.atlassian.net \
  --jira-username user@company.com \
  --jira-api-key XXXX
```

#### toolset - 도구 관리

```bash
# 모든 도구셋 상태 확인
holmes toolset status

# 특정 도구셋 상태 확인
holmes toolset status --toolset kubernetes/core
```

### 7.4 실전 예시

**예시 1: Pod 크래시 조사**

```bash
$ holmes ask "nginx Pod가 왜 계속 재시작되나?"

🔍 Investigating...

📋 Tool Calls:
  1. kubectl_get_by_name(kind=pod, name=nginx, namespace=default)
  2. kubectl_logs(pod=nginx, previous=true)
  3. kubectl_describe(kind=pod, name=nginx)

📊 Analysis:

**근본 원인:**
nginx Pod가 OOMKilled 상태로 반복 재시작 중입니다.
- 메모리 리밋: 128Mi
- 실제 사용량: 150Mi+ (리밋 초과)

**권장 조치:**
1. 메모리 리밋을 256Mi로 증가
2. nginx 설정에서 worker_processes 조정
3. 장기적으로 메모리 누수 점검 필요

💰 Cost: $0.0234 (1,543 prompt + 287 completion tokens)
```

**예시 2: PagerDuty 인시던트 자동 조사**

```bash
$ holmes investigate pagerduty \
    --pagerduty-api-key XXXX \
    --update

🔔 Found 3 open incidents

📋 Investigating: "High Error Rate on API Server"
  - Checking error logs...
  - Querying Prometheus metrics...
  - Analyzing recent deployments...

✅ Analysis written to incident P123456

📋 Investigating: "Database Connection Timeout"
  ...
```

---

## 8. K8sGPT/Kagent와 비교

### 8.1 아키텍처 비교

| 항목 | K8sGPT | Kagent | HolmesGPT |
|------|--------|--------|-----------|
| **분류** | 진단 도구 | 에이전트 프레임워크 | 조사 에이전트 |
| **LLM 역할** | 설명 생성 | 도구 선택+실행 | 도구 선택+실행 |
| **K8s 접근** | 읽기만 | 읽기/쓰기 | 읽기만 |
| **K8s 네이티브** | 아니오 | 예 (CRD) | 아니오 |
| **알람 통합** | MCP로 가능 | 없음 | 네이티브 |
| **Runbook** | 없음 | 없음 | 내장 |
| **설치 난이도** | 쉬움 | 복잡 | 쉬움 |

### 8.2 사용 시나리오

```
┌─────────────────────────────────────────────────────────────┐
│                    사용 시나리오별 추천                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  "빠른 클러스터 헬스체크"                                   │
│  → K8sGPT (규칙 기반, 빠름)                                 │
│                                                              │
│  "알람 발생 시 자동 조사"                                   │
│  → HolmesGPT (알람 통합, Runbook)                          │
│                                                              │
│  "K8s 리소스 자동 수정/배포"                                │
│  → Kagent (쓰기 권한, CRD 기반)                            │
│                                                              │
│  "ChatOps (Slack에서 K8s 관리)"                             │
│  → Botkube 또는 HolmesGPT + Slack                          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 조합 사용

```
┌─────────────────────────────────────────────────────────────┐
│                     권장 조합                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  일상 모니터링: K8sGPT                                      │
│       │                                                      │
│       ▼ (문제 감지)                                         │
│  심층 조사: HolmesGPT                                       │
│       │                                                      │
│       ▼ (수정 필요)                                         │
│  자동 수정: Kagent                                          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. 결론

### 9.1 HolmesGPT의 강점

1. **진짜 에이전트**: LLM이 도구를 선택하고 반복적으로 조사
2. **알람 통합**: PagerDuty/OpsGenie/AlertManager 네이티브 지원
3. **양방향**: 분석 결과를 원본 시스템에 자동 기록
4. **Runbook**: 체계적인 조사 절차 적용
5. **안전**: 읽기 전용, RBAC 준수
6. **쉬운 설치**: `pip install holmesgpt`

### 9.2 적합한 사용 사례

- **온콜 자동화**: 알람 발생 시 1차 분석 자동화
- **인시던트 대응**: 근본 원인 분석 가속화
- **지식 축적**: Runbook으로 조사 절차 표준화
- **비용 절감**: 반복적인 수동 조사 시간 절약

### 9.3 시작하기

```bash
# 1. 설치
pip install holmesgpt

# 2. API 키 설정
export OPENAI_API_KEY="sk-..."

# 3. 바로 사용
holmes ask "내 클러스터에서 문제 있는 Pod 찾아줘"
```

---

## 참고 자료

- [HolmesGPT GitHub](https://github.com/HolmesGPT/holmesgpt)
- [HolmesGPT 공식 문서](https://holmesgpt.dev/)
- [CNCF 블로그 - HolmesGPT](https://www.cncf.io/blog/2026/01/07/holmesgpt-agentic-troubleshooting-built-for-the-cloud-native-era/)
- [Linux Foundation 교육](https://training.linuxfoundation.org/resources/holmesgpt-ai-driven-troubleshooting-for-kubernetes/)
